export class Square {
    idx: Number;
    idy: Number;
    value: Number;
    constructor(x: number, y:number, v: Number) {
        this.idx = x;
        this.idy = y;
        this.value = v;
    }
}
